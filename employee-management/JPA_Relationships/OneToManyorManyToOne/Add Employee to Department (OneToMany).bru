meta {
  name: Add Employee to Department (OneToMany)
  type: http
  seq: 2
}

post {
  url: http://localhost:8081/api/departments/{{departmentId}}/employees
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Priya",
    "email": "priya@test.com"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
