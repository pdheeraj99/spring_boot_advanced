meta {
  name: Unlink Employee from Project (ManyToMany)
  type: http
  seq: 6
}

delete {
  url: http://localhost:8081/api/projects/{{projectId}}/employees/{{employeeId}}
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
